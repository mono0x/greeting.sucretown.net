%header.page-header
  %h1 スケジュール

.row
  - months.each_with_index do |month, index|
    - dates = schedules.by_month(month).map(&:date)
    %section.col-sm-6.col-md-4
      != calendar(month, caption: month.strftime('%Y/%m')) {|date| dates.include?(date) && { href: date.strftime('/schedule/%Y/%m/%d/') } }
    - if index % 3 == 2
      .clearfix.visible-md-block.visible-lg-block
    - if index % 2 == 1
      .clearfix.visible-sm-block
